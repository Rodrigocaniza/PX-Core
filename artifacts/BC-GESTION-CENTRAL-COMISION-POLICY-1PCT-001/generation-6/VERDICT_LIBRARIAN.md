# Verdict — Librarian, generación 6
Runner: LIBRARIAN-IND-COMISION-POLICY-1PCT-006
Snapshot: a5d6955828850b322c7ea00f5b46e3b5e7f3d7e4
Veredicto: FAIL

## Qué verifiqué y cómo

**Pruebas (cifras 395 / 195 / 144 / 112 / 8 / 24).**
- `python -m pytest -q` → `395 passed in 43.60s`. Coincide con SUMMARY.md, TEST_EVIDENCE.md, ARTIFACT_CONSISTENCY.md y `WORKFLOW.tests.full_regression`.
- `python -m pytest tests/gestion_central -q` → `195 passed in 35.36s`.
- Recuento real por archivo (`--collect-only`, agrupado por fichero): `test_comisiones.py` **112**, `test_comisiones_ui_interactions.py` **8**, `test_comision_rate_boundary.py` **24** → **144**. Coincide exactamente con el «112 + 8 + 24» de TEST_EVIDENCE.md.
- Funciones reales (`grep -c "^def test_"`): 94 / 8 / 24. `@pytest.mark.parametrize` en `test_comisiones.py`: **6**. 94 + 18 = 112, consistente con «94 funciones, seis parametrizadas, 18 casos extra».
- 144 − 51 = 93 casos aportados por la misión, y 302 + 93 = 395: aritmética interna correcta.

**Base de la misión.** `git cat-file -t e7732603…` → commit; `git rev-parse 578bf8b^` → `e7732603d9eb098867a272598e6d30803a4f1ac3`, es decir el padre directo del snapshot de la generación 1 y ancestro de HEAD. Lo declarado en SUMMARY.md, MISSION_LEASE.json y WORKFLOW.json es exacto.

**La tasa se fija sólo en `approve()` y `mark_paid()`.** Contrastado contra el código, no contra los documentos:
- `modulos/gestion_central/comisiones.py:664` `_pin_rated_period` es el único escritor en caliente de `commission_rated_periods`; se invoca desde `_transition(..., pin_period=True)` y `grep -n "pin_period"` da exactamente dos llamadores: `approve()` (línea 738) y `mark_paid()` (línea 750). `recalculate()` (línea 924) no lo llama ni escribe esa tabla.
- `decide()` (línea 217) resuelve primero por `pinned_for(period)` y sólo después por el catálogo.
- `set_general_rate` (línea ~840) ya no bloquea: la única guarda es que la vigencia no retroceda, y asienta los períodos protegidos que quedan fuera de su alcance.

**Escenario propio, sin reusar las pruebas del paquete** (base temporal, API pública):
- Venta con fecha tipeada `2136-04-10`, sólo calculada → `commission_rated_periods` vacío y `policy_for_period(...)["pinned"] is False`. Publicar 50% con vigencia `2100-01-01` + `recalculate` la corrige a `rate_bp=5000` / 200.000 Gs. **Un cálculo provisional no fija nada y sigue siendo corregible.**
- Venta en `2099-04` llevada a `APROBADA` → `{'2099-04': 100}`. Publicar 90% con vigencia posterior no la mueve; una vigencia hacia atrás se rechaza («la vigencia no puede retroceder»). Una venta nueva del mismo mes cobra **100 bp / 10.000 Gs.**, la tasa fijada. `observe()` sobre la aprobada **no** desfija.

**Siembra de la migración vs. MIGRATION.md.** `repository.py:280 _backfill_rated_periods`: el `SELECT` filtra `e.policy_status = CANONICA_APROBADA AND e.status IN ('APROBADA','PAGADA') AND COALESCE(s.voided,0)=0`; agrupa por período; con más de una tasa oficial asienta `COMMISSION_PERIOD_RATE_SEED_SKIPPED` con motivo `EVIDENCIA_DISCREPANTE` y no fija; desempata pago sobre aprobación y, a igualdad, el más antiguo; `INSERT OR IGNORE`; `_audit_seed_once` evita re-asentar. No hay un solo `UPDATE`/`INSERT` sobre `commission_entries` en esa función. Todo lo que MIGRATION.md «Paso 5» declara es cierto.

**Captura y rótulo.** Hash y dimensiones reales del PNG: `93725` bytes, `1c9a1e5e5fafec09cd55d1c2d619761240d7abedaf33a14fe83a49f702b07906`, `1920×1080`, modo `RGB` — idénticos a lo declarado en VISUAL_EVIDENCE.md. El rótulo citado es literalmente el que produce `modulos/gestion_central/comisiones_ui.py:259-264`: `" · fijada al aprobarse o pagarse"` cuando `policy["pinned"]`, y `" · provisional: aún sin aprobación ni pago en el período"` si no; la rama sin tasa (líneas 251-255) emite el texto exacto que VISUAL_EVIDENCE transcribe, y el KPI «COMISIÓN OFICIAL — SIN TASA EN VIGOR». Inspeccioné la imagen: encabezado, KPIs `4.345.000` / `43.450`, las tres vendedoras (620.000→6.200, 1.425.000→14.250, 2.300.000→23.000), S-301 / S-302 / S-103, el desglose de cuatro líneas, la nota de política, el historial con `SALE_REGISTERED`/`COMMISSION_RECALCULATED`, la ausencia de banda de aviso, los seis estados y los siete controles. Todo lo que VISUAL_EVIDENCE.md afirma sobre la captura es verdad.

**MANIFEST y ZIP.** `MANIFEST.sha256` tiene **41** entradas. Ejecutado desde la raíz del worktree, `sha256sum -c` da 38 OK y 3 FAILED (`PROMPT_AUDITOR/LIBRARIAN/QA.txt`). Verifiqué que esos tres están modificados en el árbol de trabajo **después** del snapshot (`git status` los marca `M`) y que el manifest sí corresponde al snapshot: el SHA-256 del blob de HEAD con finales CRLF coincide dígito a dígito con el manifest para los tres. Contra el snapshot, **41/41 OK**. No es un hallazgo del paquete.
ZIP: **42 miembros** = los 41 del manifest + `MANIFEST.sha256`. Verificado miembro a miembro en Python: los 42 son byte-idénticos al snapshot (los 39 no tocados coinciden con el worktree; los 3 prompts coinciden con HEAD). 0 mismatch, 0 entradas del manifest ausentes del ZIP.

**Invariantes de ARCHITECTURE_DELTA.md.** Numerados **1..11**, sin huecos ni duplicados. Los contrasté uno a uno contra el código: 1 (`INSERT INTO commission_policies` sólo con `CANONICAL_SCOPE`), 2, 3, 4 (`_require_current_policy` como guard de `review`/`approve`/`mark_paid`), 5 (verificado con mi escenario propio), 6 (`in_force_for`), 7 (`replaced` en `recalculate` línea 1008 **y** en `_apply_source_update` línea 449, mismo nombre de bloque), 8 (`non_official_amount` separado en `report` y export), 9, 10 (siembra), 11 (`COMMISSION_PERIOD_RATE_PINNED` / `_SEEDED` / `_SEED_SKIPPED`, los tres idempotentes). Ninguno es económicamente falso; el 9 tiene una imprecisión que anoto como observación.

**Otros contrastes.** `SINTETICA_PENDIENTE_APROBACION` aparece en el código una sola vez, en `comision_policy.py:42` dentro de `RETIRED_POLICY_STATUSES`. La tabla de estados de política de COMMISSION_POLICY_1PCT.md tiene los cuatro de `POLICY_STATUSES` y ninguno de más. `contract_version: 3` con bloque `policy` por período (`pinned`, línea 795), `current_policy` aparte y `_policy_disclaimer` propio. Code spans balanceados en los 25 `.md` (el único caso «impar» es un span partido en dos líneas en MIGRATION.md, válido en Markdown). Backlog: HANDOFF.md numera 29 hallazgos abiertos y `WORKFLOW.non_blocking_findings_recorded` tiene 29, uno a uno equivalentes; la partición 14+8+1+4+2 declarada en ARTIFACT_CONSISTENCY coincide con la numeración real. INDEPENDENCE: conté las observaciones de la generación 5 en los verdicts — Librarian 7, QA 4 (O1-O4), Auditor 6 = **17**, como declara. Los **quince** verdicts de `generation-1/`…`generation-5/` existen y `git log` muestra **un solo commit por fichero**: ninguno fue retocado nunca. `python tools/check_mission_package_consistency.py …` → `PAQUETE CONSISTENTE` (exit 0).

## Bloqueantes

1. **L1-g6 — `ARTIFACT_CONSISTENCY.md:15-18`: el recuento de finales de línea es falso, y el documento se clasifica mal a sí mismo.**
   Afirma: «en un clon nuevo **33 de los 41 ficheros** —24 `.md`, siete `.py` y los dos `.json`— llegan con CRLF […] Los ocho restantes no cambian: siete ya traen CRLF en el worktree —`ARTIFACT_CONSISTENCY.md`, `COMMISSION_RULES.md`, los tres `PROMPT_*.txt` y las dos herramientas de `tools/`— y el octavo es el PNG».
   Real, medido byte a byte sobre los 41 ficheros del manifest: **34** son LF puro (**25** `.md`, 7 `.py`, 2 `.json`) y sólo **6** traen CRLF (`tools/capture_gestion_central_comisiones.py`, `tools/check_mission_package_consistency.py`, `COMMISSION_RULES.md` y los tres `PROMPT_*.txt`). `ARTIFACT_CONSISTENCY.md` tiene **CR = 0** (`tr -cd '\r' < ARTIFACT_CONSISTENCY.md | wc -c` → `0`), es decir es uno de los LF y no uno de los CRLF que se atribuye. La generación 6 reescribió este documento con finales LF y no actualizó su propio recuento. Es una cifra declarada que no coincide con la real y una autoclasificación demostrablemente falsa.

2. **L2-g6 — `HANDOFF.md:163-168` (hallazgo 29) y `WORKFLOW.json:328`: reliquia de «fijada al tarifarse» presentada como el rótulo real y como trabajo pendiente.**
   HANDOFF declara, en tiempo presente y dentro del backlog de hallazgos «abiertos y **no** corregidos»: «**`VISUAL_EVIDENCE.md` quedó desactualizado**: el capturador tarifa el período, de modo que la cabecera real ahora incluye « · fijada al tarifarse» y la captura de la generación 3 ya no coincide. […] Debe regenerarse en la generación 6».
   Falso por dos vías: (a) el rótulo que produce el código es `" · fijada al aprobarse o pagarse"` (`modulos/gestion_central/comisiones_ui.py:259`) — la cadena «fijada al tarifarse» no existe en ninguna parte del código; (b) VISUAL_EVIDENCE.md y su captura **ya fueron regenerados en esta generación**, como el propio HANDOFF dice 60 líneas más abajo («`VISUAL_EVIDENCE.md` y su captura se regeneraron (`L6-g5`)»). El paquete se contradice a sí mismo y conserva la redacción anterior.

3. **L3-g6 — `HANDOFF.md:118-124` (hallazgo 25) y `WORKFLOW.json:324`: declaran vigente un daño económico que la generación 6 eliminó.**
   Afirman: «el problema de fondo **sigue abierto y empeoró**: ahora la fecha errónea fija ese mes para siempre y lo hace pagar mal en silencio. Está registrado como el bloqueante económico `AB2-g5`» (WORKFLOW: «ahora fija ese mes para siempre y lo hace pagar mal en silencio. Ver el bloqueante economico AB2-g5»).
   Demostradamente falso contra el código y contra mi propio escenario: una venta fechada `2136-04-10` que sólo se calcula no escribe fila en `commission_rated_periods`, y publicar una vigencia posterior + `recalculate` la corrige. `_pin_rated_period` sólo se alcanza desde `approve()`/`mark_paid()`. Además el mismo `WORKFLOW.blockers_open` marca `AB2-g5` como `CERRADO_EN_GEN6_PENDIENTE_DE_VERDICTS`, de modo que el backlog contradice al registro de bloqueantes del mismo fichero.

4. **L4-g6 — `HANDOFF.md:103-108` (hallazgo 23) y `WORKFLOW.json:322`: cifras de CRLF congeladas en un paquete que ya no existe.**
   Afirman: «un checkout limpio devuelve **20 de los 40** ficheros con CRLF —**18 de los 26** `.md` y **dos** `.py`—».
   Real: el manifest tiene **41** ficheros, no 40; en un checkout limpio con `core.autocrlf=true` dejarían de coincidir **34** (los 25 `.md` con LF, los **7** `.py` y los 2 `.json`), no 20. Ninguno de los cuatro números declarados es correcto. El Librarian de la generación 5 ya había abierto exactamente este bloqueante («hallazgo 23 de WORKFLOW congelado en “21 de 33”») y la corrección volvió a quedar desfasada.

5. **L5-g6 — Reliquias textuales de «fijar al tarifarse» que la misión pedía eliminar de todo documento del paquete.**
   - `TEST_EVIDENCE.md:208-210`: «Desde la generación 5 esa deriva no es alcanzable publicando —**el período queda fijado al tarifarse**—». Falso desde esta generación: el período queda fijado al aprobarse o pagarse, y un período sólo calculado sí es alcanzable publicando (lo reproduje: `rate_bp` pasa de 100 a 5000 tras publicar y recalcular).
   - `HANDOFF.md:158` (hallazgo 28): «con la marca `pinned` de si **quedó fijado al tarifarse**». La marca `pinned` significa hoy que existe fila en `commission_rated_periods`, que sólo se escribe en el boundary `APROBADA`/`PAGADA`.

## Observaciones no bloqueantes

1. **`ARCHITECTURE_DELTA.md`, invariante 9: «La frontera es un solo predicado —`RATING_BOUNDARY_STATES`— y se aplica igual en caliente y en la migración».** El efecto es correcto, pero la constante no gobierna nada: `RATING_BOUNDARY_STATES` (`comisiones.py:56`) sólo aparece en un docstring (`comisiones.py:206`) y en las pruebas. En caliente la frontera son los dos `pin_period=True` literales de `approve`/`mark_paid`, y en la migración es el literal SQL `e.status IN ('APROBADA','PAGADA')` (`repository.py:326`). Son dos sitios con el mismo valor, no un predicado único; si alguien cambiara la constante, ni el código en caliente ni la migración cambiarían.
2. **`repository.py:182-183`, comentario del `CREATE TABLE`:** «Evidencia durable de que a un período se le aplicó una tasa. Una fila por período, escrita **la primera vez que se tarifa**…». Reliquia de la semántica de la generación 5 dentro del propio código. No es una afirmación del paquete documental, pero contradice la regla nueva justo en el sitio donde un lector la buscaría.
3. **`SAFE_PAUSE.md:112-113`** también dice «el rótulo […] ahora incluye « · fijada al tarifarse»» y «Debe regenerarse en GEN6». A diferencia de L2-g6 no lo cuento como bloqueante porque el documento entero está explícitamente fechado en la pausa (`HEAD en la pausa 998037f`, «Estado del código en la pausa», «Regresión 371/371») y se lee como acta histórica, no como estado vigente.
4. **`TEST_EVIDENCE.md:230`** cita, en «Pruebas preexistentes actualizadas», que `test_structured_export_has_stable_contract_and_no_customer_data` verifica `contract_version: 2`. Es la anotación histórica de la generación 3 y el mismo documento la corrige a 3 quince líneas más abajo, pero conviven dos cifras para la misma prueba.
5. **`generation-6/` no existe todavía** en el snapshot, de modo que la verificación «que contenga los tres verdicts» no puede satisfacerse ahora: este verdict es uno de esos tres. `ARTIFACT_CONSISTENCY.md` lo enuncia correctamente en futuro.
6. **Los tres `PROMPT_*.txt` están modificados en el árbol de trabajo respecto del snapshot** (secciones de generación 6 añadidas por la orquestación después del commit). No es un defecto del paquete —el manifest y el ZIP corresponden al snapshot—, pero significa que un `sha256sum -c` corrido tal cual en este worktree da 3 FAILED engañosos.

## Superficie que mi revisión NO cubrió

- **No audité la corrección económica bajo concurrencia.** No probé dos `approve()` simultáneos sobre el mismo período, ni `approve` contra `set_general_rate`, ni `approve` contra `recalculate`. Verifiqué que `_pin_rated_period` corre dentro del `BEGIN IMMEDIATE` de `_transition`, pero eso es lectura de código, no una prueba de carrera. Es la superficie del Auditor.
- **No intenté romper el boundary.** No busqué rutas por las que un período quede fijado sin hecho económico oficial, ni por las que un período fijado cobre otra tasa. Sólo comprobé los caminos felices y los tres o cuatro escenarios que documenté arriba.
- **No verifiqué la aritmética monetaria por mi cuenta.** Acepté que `comision_policy.py` no cambió sin comparar su contenido contra la generación 3, y no reproduje los bordes de redondeo `HALF_UP` ni busqué floats.
- **No construí bases legadas a mano** para ejercitar `_backfill_rated_periods`. Leí la función y confié en que las 7 pruebas dirigidas del grupo «Migración segura» cubren lo que dicen cubrir; no verifiqué que cada una de esas 7 realmente afirme lo que su fila de la tabla promete, sólo que el archivo aporta 24 casos y que todos pasan.
- **No verifiqué el contenido de las 144 pruebas una a una.** Contrasté nombres citados en TEST_EVIDENCE.md de forma muestral, no exhaustiva: no descarto que alguna prueba nombrada en las tablas no exista o no demuestre exactamente lo que la fila afirma.
- **No revisé los quince verdicts de las generaciones 1 a 5 por contenido**, sólo que existen, que no fueron retocados y que INDEPENDENCE cuenta bien sus observaciones de la generación 5.
- **No comprobé el rótulo de la captura por OCR ni pixel a pixel**: leí la imagen y comparé lo que se ve con lo transcrito. Una diferencia tipográfica sutil se me habría escapado.
- **No evalué el ZIP fuera de este worktree**, ni reproduje el escenario del hallazgo 23 haciendo un clon limpio: las cifras de finales de línea las derivé de los bytes reales del worktree y de `core.autocrlf=true`, no de un checkout nuevo.
