# Verdict — Librarian, generación 7
Runner: LIBRARIAN-IND-COMISION-POLICY-1PCT-007
Snapshot: 41131a6a111be6e33ad1d47497bf22b128faf6e3
Veredicto: FAIL

## Qué verifiqué y cómo

**Pruebas.**
- `python -m pytest -q` → `418 passed in 51.26s`. Coincide con el 418/418 declarado (el tiempo de 39,93 s de `TEST_EVIDENCE.md` es de otra máquina; no es una afirmación verificable).
- `python -m pytest tests/gestion_central -q` → `218 passed in 42.36s`. Coincide.
- Recuento real por archivo, con `--collect-only -q`: `test_comisiones.py` 112, `test_comisiones_ui_interactions.py` 8, `test_comision_rate_boundary.py` 24, `test_comision_period_unpin.py` 23. Suma **167**, exactamente 112 + 8 + 24 + 23. Coincide con `TEST_EVIDENCE.md` y `ARTIFACT_CONSISTENCY.md`.
- La tabla de grupos de la generación 7 (`TEST_EVIDENCE.md:36-53`) suma 1+1+1+2+1+1+1+3+2+1+2+4+1+2 = **23**, que es el recuento real del archivo dirigido.
- 302 de línea base + 116 = 418, y 26 (g5) + 26 (g6) + 23 (g7) = 75 de las 116; aritmética consistente.

**Escritores del libro `commission_period_rate_events`.** `grep -rn "_record_period_rate_event\|_record_seed_event" --include=*.py`:
- definición en `modulos/gestion_central/comisiones.py:754`; llamadores en `comisiones.py:804` (`_pin_rated_period`) y `comisiones.py:836` (`_reconcile_period_pin`) — **dos**, no tres;
- `modulos/gestion_central/repository.py:384` `_record_seed_event` hace su propio `INSERT INTO commission_period_rate_events` (línea 388) — es un **segundo escritor**. Ver bloqueante L1-g7.
- `INSERT` de este libro en todo el árbol de producción: exactamente dos (comisiones.py:766 y repository.py:388). Ningún `UPDATE` ni `DELETE` sobre la tabla en `modulos/` ni en `tools/`. En pruebas sí hay `DELETE` de fixture (observación 1).

**Boundary de salida.** `comisiones.py:721` invoca `self._reconcile_period_pin(con, entry["period"], actor, action)` dentro de `_set_status`, después del `UPDATE` de estado; `_transition` llama `_pin_rated_period` después, así que aprobar no se autodesfija. `_reconcile_period_pin` (812-846) sólo escribe si el último evento es `PINNED` y `_live_official_facts` está vacío; `_live_official_facts` (729-752) cuenta `paid_at IS NOT NULL` **o** `status IN ('APROBADA','PAGADA')` con venta no anulada, de modo que una `PAGADA` viva nunca suelta. Verificado también por los 23 casos dirigidos.

**Migración.** `repository.py:_backfill_period_rate_events` (306-381) sólo inserta `'PINNED'` (literal en el SQL de `_record_seed_event`); no hay ninguna ruta que escriba `UNPINNED` en la migración. No escribe sobre `commission_entries`. Los descartes van a `COMMISSION_PERIOD_RATE_SEED_SKIPPED` vía `_audit_seed_once`.

**`RATING_BOUNDARY_STATES` / `BOUNDARY_SQL_IN`.** Definidos en `comision_policy.py:43-44`, con `BOUNDARY_SQL_IN` derivado de la tupla. Gobiernan de verdad los dos lados: `comisiones.py:749` (`_live_official_facts`), `repository.py:345` (la siembra) y `comisiones.py:794` (`_pin_rated_period` levanta `ValueError` si el boundary no está en la tupla). La observación 1 del Librarian de la generación 6 queda atendida.

**Invariantes.** `ARCHITECTURE_DELTA.md` numera 1..14 sin huecos ni duplicados. Contrastados contra el código: 1 (sólo `CANONICAL_SCOPE` se escribe, `comisiones.py:1034-1046`), 5 y 9 (boundary compartido; `set_general_rate` ya no bloquea, sólo calcula `protected` para auditoría, `comisiones.py:1027`), 10 (`_reconcile_period_pin` desde `_set_status`), 12 (ningún `UNPINNED` en la siembra), 13 (las cuatro acciones de auditoría existen), 14 (`_require_official_and_live` = `_reject_voided_sale` + `_require_current_policy`, usado como `guard` en `review`, `approve` y `mark_paid`, líneas 873/882/895). No hallé ninguno falso.

**MANIFEST y ZIP.** `MANIFEST.sha256` lista **45** ficheros (29 `.md`, 10 `.py`, 3 `.txt`, 2 `.json`, 1 `.png`). `sha256sum -c` desde la raíz del worktree: **42 OK, 3 FAILED** — los tres `PROMPT_*.txt`. Comprobé el origen: el ZIP y el manifest coinciden entre sí en esos tres ficheros, y su contenido es idéntico al de `HEAD` salvo finales de línea; los ficheros del worktree fueron editados **después** de empaquetar, para añadirles el párrafo «GENERACION 7» de mi propio encargo (`git diff` muestra exactamente eso, 3 ficheros, 1 línea cada uno). El paquete no es responsable de esa divergencia: observación 2, no bloqueante. ZIP: **46 miembros** (los 45 del manifest más `MANIFEST.sha256`), verificados uno a uno por SHA-256 contra el worktree: 43 idénticos, y los 3 prompts explicados arriba.

**Finales de línea.** Recuento real sobre los 45: **6** ficheros ya traen CRLF en el worktree (`tools/capture_gestion_central_comisiones.py`, `tools/check_mission_package_consistency.py`, `COMMISSION_RULES.md` y los tres `PROMPT_*.txt`), **1** es binario (PNG) y **38** llegan sólo con LF, repartidos en 28 de los 29 `.md`, 8 `.py` y los 2 `.json`. Es exactamente lo que declara `ARTIFACT_CONSISTENCY.md`. No lo es lo que declara el backlog: ver bloqueante L4-g7. `core.autocrlf` = `true`, confirmado.

**Captura.** `screenshots/comision-1pct-1920x1080.png`: 93.665 bytes, SHA-256 `f0919e28ac9a60911357ab6db29c1af9e374cc77a2b1038e522de2f078d93dc6`, cabecera IHDR 1920×1080, color type 2 (RGB). Los tres datos coinciden con `VISUAL_EVIDENCE.md`. Abrí la imagen: el rótulo es «Comisión oficial 1,00% de la base · COMISION_GENERAL_1PCT v1 · vigente desde 2026-08-01 · fijada al aprobarse o pagarse · redondeo HALF_UP a Gs. enteros», que es literalmente lo que compone `comisiones_ui.py:263-266` con la rama `policy.get("pinned")` de la línea 261. Las otras dos formas citadas también son literales del código (líneas 252-255 y 262). KPIs `4.345.000` / `43.450`, desglose 500.000 − 25.000 = 475.000 → 4.750, las tres vendedoras 6.200/14.250/23.000, los seis estados y la ausencia de banda de aviso: todo como se describe.

**Backlog.** 29 hallazgos numerados en `HANDOFF.md` y 29 en `WORKFLOW.non_blocking_findings_recorded`, en el mismo orden y con el mismo contenido item a item. Son idénticos — y comparten el mismo error de cifras (L4-g7).

**Otros.** `python tools/check_mission_package_consistency.py artifacts/BC-GESTION-CENTRAL-COMISION-POLICY-1PCT-001` → `PAQUETE CONSISTENTE`, exit 0. Los tres verdicts de `generation-6/` tienen un solo commit cada uno (`8e049e2`) y no fueron retocados; lo mismo los quince de `generation-1..5`. La generación 7 toca 8 ficheros fuera de `artifacts/` (4 de `modulos/gestion_central/` + 4 de `tests/gestion_central/`), como declara `ARTIFACT_CONSISTENCY.md`. `SINTETICA_PENDIENTE_APROBACION` no aparece en código fuera de la lista de retiradas.

## Bloqueantes

**L1-g7 — `ARCHITECTURE_DELTA.md:55-57` (y sus ecos) afirman un único escritor con tres llamadores; hay dos escritores y dos llamadores.**
Afirmación: «Lo escribe un solo método —`_record_period_rate_event`— desde tres llamadores: `_pin_rated_period` (`approve`/`mark_paid`), `_reconcile_period_pin` (el boundary de salida) y la siembra de la migración.»
Evidencia: `_record_period_rate_event` está definido en `modulos/gestion_central/comisiones.py:754` y tiene exactamente **dos** llamadores (`comisiones.py:804` y `comisiones.py:836`). La siembra de la migración **no lo llama**: `modulos/gestion_central/repository.py:384` define `_record_seed_event`, que ejecuta su propio `INSERT INTO commission_period_rate_events(...)` en `repository.py:388`. Son dos rutas de escritura independientes, en dos clases distintas, con dos formatos de asiento distintos (`repository.audit(... "COMMISSION_PERIOD_RATE_SEEDED" ...)` frente a `"COMMISSION_PERIOD_RATE_PINNED"`). La propiedad que el paquete usa para sostener que «la secuencia `PINNED → UNPINNED → PINNED` de un período está completa» (docstring de `comisiones.py:754-762`) no está garantizada por un único escritor, sino por dos que hoy coinciden.
Mismos ecos falsos: `HANDOFF.md:337-338` («Escribir el libro es competencia de un único método, `_record_period_rate_event`, con tres llamadores»), `ARTIFACT_CONSISTENCY.md:96` («`_record_period_rate_event` como único escritor») y `WORKFLOW.json:760` (`"single_writer": "CommissionService._record_period_rate_event"`).
Nota: el comentario del código en `comisiones.py:725-728` sí es cierto, porque se acota a «ningún otro sitio **del módulo**». La documentación borra esa acotación y con ella la afirmación deja de ser verdadera.

**L2-g7 — `MIGRATION.md:113`, `ARCHITECTURE_DELTA.md:47`, `HANDOFF.md:330` y `WORKFLOW.json:759` afirman que `commission_rated_periods` «no se lee»; la migración la lee.**
Afirmación: «La tabla `commission_rated_periods` de la generación 5 queda **congelada**: no se lee ni se escribe, y no se borra.»
Evidencia: `modulos/gestion_central/repository.py:356` ejecuta `SELECT period FROM commission_rated_periods` y guarda el resultado en `legacy`, que se usa en el bucle de `repository.py:377-381` para emitir `COMMISSION_PERIOD_RATE_SEED_SKIPPED` con motivo `SIN_HECHO_ECONOMICO_VIVO`. La lectura no es incidental: es la que hace posible la frase inmediatamente siguiente del propio `MIGRATION.md:113-115` («Sus filas sin evidencia viva no se arrastran, y cada descarte queda asentado»), que sin leer la tabla no podría cumplirse. El documento se contradice a sí mismo en dos frases consecutivas, y la primera es la falsa. «No se escribe y no se borra» sí es cierto; «no se lee» no.

**L3-g7 — `MIGRATION.md:107` declara un mecanismo de idempotencia que el código no usa, y es una reliquia de la representación anterior.**
Afirmación: «| **Es idempotente** | `INSERT OR IGNORE` por período; ni la siembra ni el descarte se re-asientan si su asiento ya existe |».
Evidencia: la siembra usa un `INSERT` normal (`repository.py:386-397`); no hay ningún `INSERT OR IGNORE` sobre `commission_period_rate_events` en todo el árbol, y la tabla no tiene `period` como clave primaria (`repository.py:205-215`: `id INTEGER PRIMARY KEY AUTOINCREMENT`), así que `INSERT OR IGNORE` no podría producir idempotencia por período aunque se usara. La idempotencia real proviene del conjunto `seeded` calculado en `repository.py:354-355` y del `continue` de la línea 359. `INSERT OR IGNORE por período` describe la tabla congelada de la generación 5, cuya clave primaria sí era `period`. El propio `HANDOFF.md:348-350` dice lo correcto y contrario: «el libro usa `INSERT` normal y la idempotencia sale de consultar el último evento», por lo que además es una contradicción interna.

**L4-g7 — el backlog declara cifras de finales de línea que no son las reales, en el mismo hallazgo que exige recalcularlas.**
Afirmación: `HANDOFF.md:103-107` — «un checkout limpio reescribe los **34** de los **41** ficheros que hoy llevan LF —25 `.md`, 7 `.py` y los 2 `.json`—»; idéntica en `WORKFLOW.json:382`.
Evidencia: el manifest de esta generación fija **45** ficheros, no 41, y el recuento real sobre ellos es **38** con LF (28 `.md`, 8 `.py`, 2 `.json`), 6 ya con CRLF y 1 binario — que es justamente lo que `ARTIFACT_CONSISTENCY.md:14-25` declara bien. Las cifras del backlog son las de la generación 6. El propio texto del hallazgo dice «Estas cifras se recalculan en cada generación que toque el paquete: quedaron desfasadas en la 5 y otra vez en la 6, y por eso el bloqueante `L4-g6` volvió a abrirse»; en la 7 vuelven a estar desfasadas. Dos documentos del paquete declaran hoy dos recuentos incompatibles del mismo hecho, y uno de ellos es falso.

**L5-g7 — `TEST_EVIDENCE.md:118` describe pruebas vivas contra la tabla congelada.**
Afirmación: «| Comisión provisional sin tasa fijada | calcular y revisar no escriben `commission_rated_periods` | 2 |».
Evidencia: las dos pruebas son `test_el_primer_calculo_no_fija_la_tasa_del_periodo` (`tests/gestion_central/test_comision_rate_boundary.py:81`) y `test_revisar_tampoco_fija_porque_todavia_no_hay_hecho_economico` (línea 92). Ambas comprueban vía el helper `rated_periods` (líneas 53-60), cuyo `SELECT` es sobre `commission_period_rate_events`, no sobre `commission_rated_periods`. Estas 24 pruebas no son historia: forman parte del inventario de 167 casos que este paquete declara vigente, y la fila las describe verificando una tabla que la generación 7 declara congelada. Es la reliquia de representación que el encargo pide descartar. (Las menciones de `HANDOFF.md:202-263`, `ARTIFACT_CONSISTENCY.md:71`, `TEST_EVIDENCE.md:63` y `WORKFLOW.json:309/534/608/689` sí son secciones fechadas de las generaciones 5 y 6 y no las cuento aquí.)

## Observaciones no bloqueantes

1. Las pruebas borran el libro append-only para construir bases legadas: `DELETE FROM commission_period_rate_events` en `tests/gestion_central/test_comisiones.py:59`, `test_comision_rate_boundary.py:150` y `:231`, y `test_comision_period_unpin.py:322`. No contradice la documentación —que habla del código de producción, donde no hay ningún `UPDATE` ni `DELETE`— pero conviene que quien lea «nada se borra nunca» sepa que el fixture sí puede.
2. `sha256sum -c MANIFEST.sha256` da hoy 42/45, no 45/45. Las tres discrepancias son los `PROMPT_*.txt`, editados por el orquestador después de construir el paquete para añadir las instrucciones de la generación 7; el ZIP y el manifest conservan la versión anterior, byte-idéntica a `HEAD` salvo finales de línea. No es un defecto del paquete, pero sí significa que la afirmación «`sha256sum -c`: 45/45 OK» de `ARTIFACT_CONSISTENCY.md:12` sólo es reproducible en el instante del empaquetado.
3. El backlog 25 (`HANDOFF.md` y `WORKFLOW.json`) sigue diciendo «Queda una forma distinta y **abierta**, `AB1-g6`», mientras `WORKFLOW.generation.closes` declara `["AB1-g6"]` y el resto del paquete lo da por cerrado. El backlog no se actualizó al cerrar el bloqueante que él mismo enunciaba.
4. `ARTIFACT_CONSISTENCY.md:22-25` sigue justificando el hallazgo 23 con «excede el alcance de esta generación, que es sólo B1 y B2», redacción heredada de la generación 4; el alcance de la 7 no es B1/B2.
5. El texto de `ARCHITECTURE_DELTA.md:46` dice «congelada en la 7» y el comentario equivalente de `repository.py:182` dice «congelada en la 6». Sólo uno de los dos puede ser el correcto; el cambio de representación es de la 7.
6. `TEST_EVIDENCE.md:3` fija la regresión en 39,93 s; en esta máquina son 51,26 s. Un tiempo de reloj no es una propiedad del código y no debería presentarse junto a las cifras verificables.
7. Coste de L1-g7 más allá de la redacción: como la siembra no pasa por `_record_period_rate_event`, ninguna guarda futura que se añada a ese método —por ejemplo, impedir dos `PINNED` consecutivos— alcanzaría a la migración. Es la clase de divergencia que la generación 6 ya pagó una vez (`AB1-g6`).

## Superficie que mi revisión NO cubrió

- **No audité el dinero.** No reprodují los escenarios económicos del Auditor (el sobrepago de 9.900.000 Gs, el subpago de la dirección contraria, la reversión de cobro). Confié en que las 23 pruebas dirigidas pasan; no verifiqué de forma independiente que midan lo que dicen medir. Eso es competencia del Auditor de esta generación y no debe darse por hecho a partir de mi PASS parcial.
- **No construí bases legadas propias.** No ejecuté la migración sobre bases sintéticas con evidencia discrepante, con `commission_rated_periods` poblada o con pines heredados sin hecho vivo. Verifiqué el SQL de `_backfill_period_rate_events` por lectura, no por ejecución.
- **No verifiqué la interfaz ni el export en ejecución.** Comprobé que el rótulo de la captura es el que compone `comisiones_ui.py`, pero no ejecuté la UI ni inspeccioné el `contract_version: 3` real ni el `policy_disclaimer` en sus tres ramas.
- **No releí íntegros** `COMMISSION_POLICY_1PCT.md`, `SAFE_PAUSE.md` ni `INDEPENDENCE.md`; de ellos sólo verifiqué recuentos, referencias cruzadas y las frases que aparecen en mis búsquedas dirigidas. Puede quedar en ellos alguna reliquia que no haya visto.
- **No revisé las generaciones 1 a 5 en profundidad.** Confirmé que sus verdicts no fueron retocados (un commit por fichero) y leí los de la generación 6 sólo para localizar su observación 1; no reverifiqué ninguna afirmación suya.
- **No comprobé la aritmética monetaria** (`Decimal`, `HALF_UP`, bordes de medio guaraní). El paquete afirma que no cambió desde la generación 3 y no lo contrasté.
- **La verificación del ZIP y del manifest se hizo sobre este worktree**, con `core.autocrlf=true`. No cloné el repositorio en limpio para comprobar empíricamente el comportamiento de los finales de línea que el paquete describe; deduje el recuento de los bytes actuales.
