# Artifact Consistency

El manifiesto SHA-256 liga el entrypoint, dominio, repositorio, servicio,
interfaz y pruebas a la generación auditada. Los tres estados persistentes
(`MISSION_LEASE.json`, `SESSION_STATE.json`, `WORKFLOW.json`) son JSON válido y
coinciden en misión, repositorio, aislamiento y cierre seguro.

Resultado: **CONSISTENT**.
