# Handoff

Pilot file: `releases/BC-CAJA-PILOT-001.zip`.

Extract the complete `BC-Caja/` directory and run `BC-Caja.exe`. `VERSION.txt` identifies `BC-CAJA-UX-001`. Existing production data remains under `%LOCALAPPDATA%\BC\Caja`; replacing the application directory must never replace that data directory.

Suggested product validation: compare the main capture row visually with the current paper/Excel workflow and record only concrete friction under BUG, UX, MEJORA, IDEA or NO ACCIÓN.